name: Juice Shop CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  frontend-build:
    runs-on: self-hosted
    steps:
      # Restore repository cache (hash only key build files)
      - name: Restore repository cache
        uses: actions/cache@v3
        with:
          path: ./repo
          key: repo-${{ hashFiles('package-lock.json', 'frontend/package-lock.json', 'frontend/angular.json') }}
          restore-keys: repo-

      # Checkout if cache missed
      - name: Checkout repo if cache missed
        if: steps.cache.outputs.cache-hit != 'true'
        uses: actions/checkout@v3
        with:
          path: ./repo

      # Node memory setup
      - name: Set Node memory
        run: set NODE_OPTIONS=--max_old_space_size=4096

      # Cache frontend dependencies
      - name: Cache frontend
        uses: actions/cache@v3
        with:
          path: |
            repo/frontend/node_modules
            repo/frontend/.angular
          key: ${{ runner.os }}-frontend-${{ hashFiles('repo/frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-frontend-

      # Install frontend dependencies
      - name: Install frontend dependencies
        run: |
          cd repo/frontend
          npm ci --legacy-peer-deps --prefer-offline --no-audit
          cd ../..

      # Build frontend
      - name: Build frontend
        run: |
          cd repo/frontend
          npm run build -- --configuration production --watch=false
          cd ../..

  server-build:
    runs-on: self-hosted
    steps:
      # Restore repository cache (same hash key as frontend)
      - name: Restore repository cache
        uses: actions/cache@v3
        with:
          path: ./repo
          key: repo-${{ hashFiles('package-lock.json', 'frontend/package-lock.json', 'frontend/angular.json') }}
          restore-keys: repo-

      # Checkout if cache missed
      - name: Checkout repo if cache missed
        if: steps.cache.outputs.cache-hit != 'true'
        uses: actions/checkout@v3
        with:
          path: ./repo

      # Node memory setup
      - name: Set Node memory
        run: set NODE_OPTIONS=--max_old_space_size=4096

      # Cache server dependencies
      - name: Cache server
        uses: actions/cache@v3
        with:
          path: repo/node_modules
          key: ${{ runner.os }}-server-${{ hashFiles('repo/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-server-

      # Install server dependencies
      - name: Install server dependencies
        run: npm ci --prefer-offline --no-audit

      # Build server
      - name: Build server
        run: npm run build:server
        working-directory: ./repo

  sonar-analysis:
    runs-on: self-hosted
    needs:
      - frontend-build
      - server-build
    steps:
      # Restore repository cache
      - name: Restore repository cache
        uses: actions/cache@v3
        with:
          path: ./repo
          key: repo-${{ hashFiles('package-lock.json', 'frontend/package-lock.json', 'frontend/angular.json') }}
          restore-keys: repo-

      # SonarQube Scan
      - name: SonarQube Scan
        run: |
          if (Get-Command sonar-scanner -ErrorAction SilentlyContinue) {
              echo "Running SonarQube scanner..."
              set SONAR_HOST_URL=http://127.0.0.1:9000
              sonar-scanner
          } else {
              echo "sonar-scanner not found, skipping analysis."
          }
