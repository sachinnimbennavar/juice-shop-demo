name: Juice Shop CI

on:
  workflow_dispatch:
    inputs:
      deploy_only:
        description: 'Set true to run only deployment'
        required: false
        default: 'false'

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Set Node memory
      - name: Set Node memory
        run: set NODE_OPTIONS=--max_old_space_size=4096

      # CI steps (conditionally skipped if deploy_only = true)
      - name: Cache frontend
        if: ${{ inputs.deploy_only != 'true' }}
        uses: actions/cache@v3
        with:
          path: |
            frontend/node_modules
            frontend/dist
            frontend/.angular
          key: ${{ runner.os }}-frontend-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-frontend-

      - name: Cache server
        if: ${{ inputs.deploy_only != 'true' }}
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-server-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-server-

      - name: Install frontend dependencies
        if: ${{ inputs.deploy_only != 'true' }}
        run: |
          cd frontend
          npm install --legacy-peer-deps --prefer-offline --no-audit
          cd ..

      - name: Build frontend
        if: ${{ inputs.deploy_only != 'true' }}
        run: |
          cd frontend
          npm run build -- --configuration production --watch=false --verbose
          cd ..

      - name: Install server dependencies
        if: ${{ inputs.deploy_only != 'true' }}
        run: npm install --prefer-offline --no-audit

      - name: Build server
        if: ${{ inputs.deploy_only != 'true' }}
        run: npm run build:server

      - name: SonarQube Scan
        if: ${{ inputs.deploy_only != 'true' }}
        run: |
          if (Get-Command sonar-scanner -ErrorAction SilentlyContinue) {
              echo "Running SonarQube scanner..."
              set SONAR_HOST_URL=http://127.0.0.1:9000
              sonar-scanner
          } else {
              echo "sonar-scanner not found, skipping SonarQube analysis."
          }

      - name: Upload build artifacts to Nexus
        if: ${{ inputs.deploy_only != 'true' }}
        shell: powershell
        env:
          NEXUS_URL: ${{ secrets.NEXUS_URL }}
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
        run: |
          Write-Host "🧩 Preparing frontend and server zips..."
          if (Test-Path "frontend/dist") {
              Compress-Archive -Path "frontend/dist/*" -DestinationPath "frontend-build.zip" -Force
          }
          $serverPaths = @(
              "app.ts","server.ts","package.json","tsconfig.json","models","routes",
              "lib","data","views","rsn","config","build"
          ) | Where-Object { Test-Path $_ }
          if ($serverPaths.Count -gt 0) {
              Compress-Archive -Path $serverPaths -DestinationPath "server-build.zip" -Force
          }
          $base64Auth = [System.Convert]::ToBase64String([System.Text.Encoding]::ASCII.GetBytes("${env:NEXUS_USERNAME}:${env:NEXUS_PASSWORD}"))
          $headers = @{ Authorization = "Basic $base64Auth" }
          if (Test-Path "frontend-build.zip") {
              Invoke-RestMethod -Uri "${env:NEXUS_URL}/repository/juice-shop/frontend-build-${env:GITHUB_RUN_NUMBER}.zip" `
                                -Method Put -InFile "frontend-build.zip" -Headers $headers
          }
          if (Test-Path "server-build.zip") {
              Invoke-RestMethod -Uri "${env:NEXUS_URL}/repository/juice-shop/server-build-${env:GITHUB_RUN_NUMBER}.zip" `
                                -Method Put -InFile "server-build.zip" -Headers $headers
          }
          Write-Host "✅ Nexus upload completed successfully."

      # ✅ Deployment Stage (always runs)
      - name: Deploy from Nexus
        shell: powershell
        env:
          NEXUS_URL: http://127.0.0.1:8081
          NEXUS_USERNAME: admin
          NEXUS_PASSWORD: Nexus@123
          FRONTEND_DEST: C:\inetpub\wwwroot\juice-shop
          SERVER_DEST: C:\JuiceShopServer
          SERVER_PROCESS_NAME: JuiceShopServer
        run: |
          Write-Host "🟢 Starting deployment..."
          function Get-LatestZip {
              param ([string]$artifactType)
              $authHeader = [System.Convert]::ToBase64String([System.Text.Encoding]::ASCII.GetBytes("$env:NEXUS_USERNAME:$env:NEXUS_PASSWORD"))
              $headers = @{ Authorization = "Basic $authHeader" }
              $uri = "$env:NEXUS_URL/service/rest/v1/search/assets?repository=juice-shop-artifacts&name=$artifactType"
              $response = Invoke-RestMethod -Uri $uri -Headers $headers -Method Get
              if ($response.items.Count -eq 0) { return $null }
              $latest = $response.items | Sort-Object {[datetime]$_.lastModified} -Descending | Select-Object -First 1
              return $latest.downloadUrl
          }

          function Download-And-Extract {
              param ([string]$url, [string]$destination)
              if (-Not $url) { Write-Host "⚠️ No URL provided"; return }
              $zipFile = Join-Path $env:TEMP ([System.IO.Path]::GetFileName($url))
              Invoke-RestMethod -Uri $url -OutFile $zipFile -Headers @{ Authorization = "Basic " + [System.Convert]::ToBase64String([System.Text.Encoding]::ASCII.GetBytes("$env:NEXUS_USERNAME:$env:NEXUS_PASSWORD")) }
              if (-Not (Test-Path $destination)) { New-Item -ItemType Directory -Path $destination -Force | Out-Null }
              Expand-Archive -Path $zipF
