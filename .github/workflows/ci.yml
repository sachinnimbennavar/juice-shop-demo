name: Juice Shop CI + Auto Latest Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Set Node memory
      - name: Set Node memory
        run: set NODE_OPTIONS=--max_old_space_size=4096

      # 3️⃣ Cache frontend dependencies
      - name: Cache frontend
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: actions/cache@v3
        with:
          path: |
            frontend/node_modules
            frontend/dist
            frontend/.angular
          key: ${{ runner.os }}-frontend-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-frontend-

      # 4️⃣ Cache server dependencies
      - name: Cache server
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-server-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-server-

      # 5️⃣ Install frontend dependencies
      - name: Install frontend dependencies
        if: ${{ github.event_name != 'workflow_dispatch' }}
        run: |
          cd frontend
          npm install --legacy-peer-deps --prefer-offline --no-audit
          cd ..

      # 6️⃣ Build frontend
      - name: Build frontend
        if: ${{ github.event_name != 'workflow_dispatch' }}
        run: |
          cd frontend
          npm run build -- --configuration production --watch=false --verbose
          cd ..

      # 7️⃣ Install server dependencies
      - name: Install server dependencies
        if: ${{ github.event_name != 'workflow_dispatch' }}
        run: npm install --prefer-offline --no-audit

      # 8️⃣ Build server
      - name: Build server
        if: ${{ github.event_name != 'workflow_dispatch' }}
        run: npm run build:server

      # 9️⃣ Upload artifacts to Nexus (push/PR only)
      - name: Upload build artifacts to Nexus
        if: ${{ github.event_name != 'workflow_dispatch' }}
        shell: powershell
        env:
          NEXUS_URL: ${{ secrets.NEXUS_URL }}       # e.g., http://127.0.0.1:8081/repository/juice-shop-artifacts
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
        run: |
          Write-Host "[INFO] Packaging and uploading build artifacts..."
          $version = $env:GITHUB_RUN_NUMBER
          $base64Auth = [System.Convert]::ToBase64String([System.Text.Encoding]::ASCII.GetBytes("${env:NEXUS_USERNAME}:${env:NEXUS_PASSWORD}"))
          $headers = @{ Authorization = "Basic $base64Auth" }

          # Frontend ZIP
          if (Test-Path "frontend/dist") {
              Compress-Archive -Path "frontend/dist/*" -DestinationPath "frontend-build-$version.zip" -Force
          }

          # Server ZIP
          $serverPaths = @(
              "app.ts","server.ts","package.json","tsconfig.json",
              "models","routes","lib","data","views","rsn","config","build"
          ) | Where-Object { Test-Path $_ }
          if ($serverPaths.Count -gt 0) {
              Compress-Archive -Path $serverPaths -DestinationPath "server-build-$version.zip" -Force
          }

          # Upload frontend
          if (Test-Path "frontend-build-$version.zip") {
              Invoke-RestMethod -Uri "${env:NEXUS_URL}/com/example/juiceshop/frontend-build/$version/frontend-build-$version.zip" `
                                -Method Put -InFile "frontend-build-$version.zip" -Headers $headers
          }

          # Upload server
          if (Test-Path "server-build-$version.zip") {
              Invoke-RestMethod -Uri "${env:NEXUS_URL}/com/example/juiceshop/server-build/$version/server-build-$version.zip" `
                                -Method Put -InFile "server-build-$version.zip" -Headers $headers
          }

      # 🔟 Deploy stage (manual deploy picks latest version automatically)
      - name: Deploy from Nexus (latest version with auth)
        shell: powershell
        env:
          NEXUS_URL: ${{ secrets.NEXUS_URL }}
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
          FRONTEND_DEST: C:\inetpub\wwwroot\juice-shop
          SERVER_DEST: C:\JuiceShopServer
          SERVER_PROCESS_NAME: JuiceShopServer
        run: |
          Write-Host "[INFO] Starting deployment..."

          $base64Auth = [System.Convert]::ToBase64String([System.Text.Encoding]::ASCII.GetBytes("${env:NEXUS_USERNAME}:${env:NEXUS_PASSWORD}"))
          $headers = @{ Authorization = "Basic $base64Auth" }

          function Download-And-Extract {
              param([string]$url, [string]$destination)
              if (-not $url) { Write-Host "[WARN] No URL provided"; return }
              $zipFile = Join-Path $env:TEMP ([System.IO.Path]::GetFileName($url))
              Write-Host "[INFO] Downloading $url ..."
              Invoke-WebRequest -Uri $url -OutFile $zipFile -Headers $headers
              if (-not (Test-Path $destination)) { New-Item -ItemType Directory -Path $destination -Force | Out-Null }
              Expand-Archive -Path $zipFile -DestinationPath $destination -Force
              Write-Host "[INFO] Extracted to $destination"
          }

          function Get-Latest-Version {
              param([string]$repoUrl, [hashtable]$headers)
              Write-Host "[INFO] Fetching file list from $repoUrl ..."
              $html = Invoke-WebRequest -Uri $repoUrl -Headers $headers -UseBasicParsing
              $links = ($html.Links | Where-Object {$_.href -match 'frontend-build-(\d+)\.zip'}) | ForEach-Object {
                  [int]([regex]::Match($_.href, 'frontend-build-(\d+)\.zip').Groups[1].Value)
              }
              if ($links.Count -eq 0) { throw "No frontend builds found in $repoUrl" }
              return ($links | Sort-Object -Descending)[0]
          }

          # Base URLs
          $frontendRepoUrl = "${env:NEXUS_URL}/com/example/juiceshop/frontend-build/"
          $serverRepoUrl   = "${env:NEXUS_URL}/com/example/juiceshop/server-build/"

          # Detect latest version automatically
          $version = Get-Latest-Version -repoUrl $frontendRepoUrl -headers $headers
          Write-Host "[INFO] Latest version detected: $version"

          # Download & deploy frontend
          $frontendUrl = "${env:NEXUS_URL}/com/example/juiceshop/frontend-build/$version/frontend-build-$version.zip"
          Download-And-Extract -url $frontendUrl -destination $env:FRONTEND_DEST

          # Download & deploy server
          $serverUrl = "${env:NEXUS_URL}/com/example/juiceshop/server-build/$version/server-build-$version.zip"
          Download-And-Extract -url $serverUrl -destination $env:SERVER_DEST

          # Restart Node server via NSSM
          if (Get-Command nssm -ErrorAction SilentlyContinue) {
              if (Get-Service -Name $env:SERVER_PROCESS_NAME -ErrorAction SilentlyContinue) {
                  Stop-Service -Name $env:SERVER_PROCESS_NAME -Force
                  Start-Sleep -Seconds 5
              }
              if (-not (Get-Service -Name $env:SERVER_PROCESS_NAME -ErrorAction SilentlyContinue)) {
                  nssm install $env:SERVER_PROCESS_NAME "C:\Program Files\nodejs\node.exe" "$env:SERVER_DEST\build\server.js"
              }
              Start-Service -Name $env:SERVER_PROCESS_NAME
              Write-Host "[INFO] Deployment completed successfully!"
          } else {
              Write-Host "[ERROR] NSSM not installed. Install NSSM or manage Node server manually."
          }
