name: Juice Shop IIS Deployment

on:
  workflow_dispatch: {}

jobs:
  iis-deploy:
    runs-on: self-hosted  # Windows machine with IIS, ARR, iisnode

    env:
      FRONTEND_DEPLOY: C:\SSDLC\Nexus_Unzips\frontend\dist\frontend
      BACKEND_DEPLOY: C:\SSDLC\Nexus_Unzips\server
      FRONTEND_IIS: C:\inetpub\wwwroot\JuiceShopFrontend
      BACKEND_IIS: C:\inetpub\wwwroot\JuiceShopBackend
      BACKEND_URL: http://localhost/JuiceShopBackend

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy Frontend to IIS
        shell: powershell
        run: |
          Import-Module WebAdministration -ErrorAction Stop
          if (Test-Path $env:FRONTEND_IIS) {
            Get-ChildItem $env:FRONTEND_IIS -Recurse -Force | Remove-Item -Force -Recurse
          } else {
            New-Item -Force -ItemType Directory -Path $env:FRONTEND_IIS | Out-Null
          }
          Copy-Item -Path "$env:FRONTEND_DEPLOY\*" -Destination $env:FRONTEND_IIS -Recurse -Force

          $envFile = Join-Path $env:FRONTEND_IIS "assets\environment.prod.js"
          if (Test-Path $envFile) {
            (Get-Content $envFile) -replace 'apiUrl\s*:\s*["''][^"''"]*["'']', "apiUrl: '$env:BACKEND_URL'" | Set-Content $envFile
          }

          $webConfigContent = @"
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name='Angular Routes' stopProcessing='true'>
          <match url='.*' />
          <conditions logicalGrouping='MatchAll'>
            <add input='{REQUEST_FILENAME}' matchType='IsFile' negate='true' />
            <add input='{REQUEST_FILENAME}' matchType='IsDirectory' negate='true' />
          </conditions>
          <action type='Rewrite' url='/index.html' />
        </rule>
      </rules>
    </rewrite>
  </system.webServer>
</configuration>
"@
          $webConfigPath = Join-Path $env:FRONTEND_IIS "web.config"
          $webConfigContent | Set-Content -Path $webConfigPath -Encoding UTF8

      - name: Deploy Backend to IIS
        shell: powershell
        run: |
          Import-Module WebAdministration -ErrorAction Stop
          if (Test-Path $env:BACKEND_IIS) {
            Get-ChildItem $env:BACKEND_IIS -Recurse -Force | Remove-Item -Force -Recurse
          } else {
            New-Item -Force -ItemType Directory -Path $env:BACKEND_IIS | Out-Null
          }
          Copy-Item -Path "$env:BACKEND_DEPLOY\*" -Destination $env:BACKEND_IIS -Recurse -Force

          $backendWebConfig = @"
<configuration>
  <system.webServer>
    <handlers>
      <add name='iisnode' path='server.js' verb='*' modules='iisnode' />
    </handlers>
    <rewrite>
      <rules>
        <rule name='Nodejs' stopProcessing='true'>
          <match url='.*' />
          <action type='Rewrite' url='server.js' />
        </rule>
      </rules>
    </rewrite>
    <iisnode nodeProcessCommandLine='C:\Program Files\nodejs\node.exe' />
  </system.webServer>
</configuration>
"@
          $backendWebConfigPath = Join-Path $env:BACKEND_IIS "web.config"
          $backendWebConfig | Set-Content -Path $backendWebConfigPath -Encoding UTF8

      - name: Configure IIS Sites
        shell: powershell
        run: |
          Import-Module WebAdministration -ErrorAction Stop

          $frontendSite = Get-Website -Name "JuiceShopFrontend" -ErrorAction SilentlyContinue
          if ($null -ne $frontendSite) {
            Start-Website -Name "JuiceShopFrontend"
          } else {
            New-Website -Name "JuiceShopFrontend" -Port 8080 -PhysicalPath $env:FRONTEND_IIS -Force | Out-Null
          }

          $backendSite = Get-Website -Name "JuiceShopBackend" -ErrorAction SilentlyContinue
          if ($null -ne $backendSite) {
            Start-Website -Name "JuiceShopBackend"
          } else {
            New-Website -Name "JuiceShopBackend" -Port 8081 -PhysicalPath $env:BACKEND_IIS -Force | Out-Null
          }

      - name: Restart IIS
        shell: powershell
        run: |
          iisreset
          Write-Host "[INFO] IIS restarted. Juice Shop frontend and backend are live!"
