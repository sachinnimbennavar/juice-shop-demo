name: Juice Shop Manual Deployment (Frontend IIS + Backend Node.js)

on:
  workflow_dispatch: # Manual trigger

jobs:
  deploy-only:
    runs-on: self-hosted

    env:
      DEPLOY_PATH: C:\SSDLC\Nexus_Unzips
      IIS_SITE_PATH: C:\inetpub\wwwroot\JuiceShop
      IIS_APPPOOL: JuiceShopAppPool
      NODE_EXE: "C:\Program Files\nodejs\node.exe"
      NSSM_EXE: "C:\SSDLC\nssm-2.24\nssm-2.24\win64\nssm.exe"

    steps:
      # 1️⃣ Deploy Frontend to IIS
      - name: Deploy Frontend to IIS
        shell: powershell
        run: |
          Import-Module WebAdministration
          Write-Host "[INFO] Deploying frontend to IIS..."

          if (Test-Path $env:IIS_SITE_PATH) {
              Write-Host "[INFO] Clearing existing files..."
              Remove-Item -Path "$env:IIS_SITE_PATH\*" -Recurse -Force
          } else {
              New-Item -ItemType Directory -Path $env:IIS_SITE_PATH -Force | Out-Null
          }

          Copy-Item -Path "$env:DEPLOY_PATH\frontend\dist\frontend\*" -Destination $env:IIS_SITE_PATH -Recurse -Force

          if (-not (Test-Path "IIS:\AppPools\$env:IIS_APPPOOL")) {
              Write-Host "[INFO] Creating App Pool: $env:IIS_APPPOOL ..."
              New-WebAppPool -Name $env:IIS_APPPOOL | Out-Null
              Set-ItemProperty "IIS:\AppPools\$env:IIS_APPPOOL" -Name processModel.identityType -Value "ApplicationPoolIdentity"
          }

          if (-not (Test-Path "IIS:\Sites\JuiceShop")) {
              Write-Host "[INFO] Creating IIS site: JuiceShop ..."
              New-Website -Name "JuiceShop" -Port 8080 -PhysicalPath $env:IIS_SITE_PATH -ApplicationPool $env:IIS_APPPOOL
          } else {
              Write-Host "[INFO] Assigning App Pool to existing IIS site ..."
              Set-ItemProperty "IIS:\Sites\JuiceShop" -Name applicationPool -Value $env:IIS_APPPOOL
          }

          Restart-WebAppPool -Name $env:IIS_APPPOOL
          Write-Host "✅ Frontend deployed to IIS"

      # 2️⃣ Deploy Backend Node.js via NSSM
      - name: Deploy Backend Node.js Service via NSSM
        shell: powershell
        run: |
          $serviceName = "JuiceShopServer"
          $nodeExe = $env:NODE_EXE
          $serverJs = Join-Path $env:DEPLOY_PATH "build\server.js"
          $nssmExe = $env:NSSM_EXE

          # Install service if not exists
          if (-not (Get-Service -Name $serviceName -ErrorAction SilentlyContinue)) {
              Write-Host "[INFO] Installing Node.js backend service via NSSM..."
              & $nssmExe install $serviceName $nodeExe $serverJs
              & $nssmExe set $serviceName AppDirectory (Join-Path $env:DEPLOY_PATH "build")
              & $nssmExe set $serviceName AppStdout (Join-Path $env:DEPLOY_PATH "build\server-out.log")
              & $nssmExe set $serviceName AppStderr (Join-Path $env:DEPLOY_PATH "build\server-err.log")
          }

          # Start or restart service
          $svc = Get-Service -Name $serviceName
          if ($svc.Status -eq "Running") {
              Restart-Service $serviceName -Force
          } else {
              Start-Service $serviceName
          }

          Write-Host "✅ Backend Node.js service deployed via NSSM"
