name: Juice Shop Deployment

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    env:
      NEXUS_URL: http://127.0.0.1:8081
      DEPLOY_PATH_FRONTEND: C:\JuiceShopServer\frontend
      DEPLOY_PATH_SERVER: C:\JuiceShopServer

    steps:
      - name: 🧩 Checkout code
        uses: actions/checkout@v3

      - name: 🚀 Deploy latest build from Nexus
        shell: powershell
        env:
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
        run: |
          Write-Host "[INFO] Starting deployment..."
          # Prepare Basic Auth
          $pair = "${env:NEXUS_USERNAME}:${env:NEXUS_PASSWORD}"
          $base64Auth = [System.Convert]::ToBase64String([System.Text.Encoding]::ASCII.GetBytes($pair))
          $headers = @{ Authorization = "Basic $base64Auth" }
          # --- FRONTEND ---
          $frontendApi = "$env:NEXUS_URL/service/rest/v1/search?repository=JuiceShopMavenDemo&group=com.example.juiceshop&name=frontend-build"
          $frontendResp = Invoke-RestMethod -Uri $frontendApi -Headers $headers
          $latestFrontend = ($frontendResp.items.version | Sort-Object -Descending | Select-Object -First 1)
          Write-Host "[INFO] Latest frontend build: $latestFrontend"
          $frontendUrl = "$env:NEXUS_URL/repository/JuiceShopMavenDemo/com/example/juiceshop/frontend-build/$latestFrontend/frontend-build-$latestFrontend.zip"
          Invoke-WebRequest -Uri $frontendUrl -OutFile "frontend.zip" -Headers $headers
          Expand-Archive -Path "frontend.zip" -DestinationPath "$env:DEPLOY_PATH_FRONTEND" -Force
          Write-Host "[SUCCESS] Frontend deployed."
          # --- SERVER ---
          $serverApi = "$env:NEXUS_URL/service/rest/v1/search?repository=JuiceShopMavenDemo&group=com.example.juiceshop&name=server-build"
          $serverResp = Invoke-RestMethod -Uri $serverApi -Headers $headers
          $latestServer = ($serverResp.items.version | Sort-Object -Descending | Select-Object -First 1)
          Write-Host "[INFO] Latest server build: $latestServer"
          $serverUrl = "$env:NEXUS_URL/repository/JuiceShopMavenDemo/com/example/juiceshop/server-build/$latestServer/server-build-$latestServer.zip"
          Invoke-WebRequest -Uri $serverUrl -OutFile "server.zip" -Headers $headers
          Expand-Archive -Path "server.zip" -DestinationPath "$env:DEPLOY_PATH_SERVER" -Force
          Write-Host "[SUCCESS] Server deployed."
          Write-Host "✅ Deployment of latest build completed successfully!"

      # --- Optimized backend install and start ---
      - name: ⚡ Install & Start Backend
        shell: powershell
        run: |
          cd $env:DEPLOY_PATH_SERVER
          Write-Host "[INFO] Installing backend dependencies (without postinstall)..."
          npm install --legacy-peer-deps --ignore-scripts

          Write-Host "[INFO] Building frontend..."
          cd frontend
          npm install --legacy-peer-deps
          npm run build:frontend
          cd ..

          Write-Host "[INFO] Building backend..."
          npm run build:server

          Write-Host "[INFO] Starting backend server..."
          Start-Process -FilePath "npm" -ArgumentList "run start" -NoNewWindow
          Write-Host "[SUCCESS] Backend server started."

      # --- Deploy frontend to IIS ---
      - name: 🖥 Deploy frontend to IIS
        shell: powershell
        run: |
          Import-Module WebAdministration
          $siteName = "JuiceShopFrontend"
          $frontendPath = "$env:DEPLOY_PATH_FRONTEND"
          Write-Host "[INFO] Deploying frontend to IIS..."
          if (Test-Path "IIS:\Sites\$siteName") {
              Remove-WebApplication -Name $siteName
          }
          New-WebApplication -Name $siteName -Site "Default Web Site" -PhysicalPath $frontendPath -ApplicationPool "DefaultAppPool"
          Write-Host "[SUCCESS] Frontend deployed to IIS."
